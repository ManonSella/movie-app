<h1><%= @user.name %></h1>
<p><%= mail_to @user.email %></p>
<div class="action">
  <%= link_to "Edit your account", edit_user_path(@user)%>
  <%= link_to "Delete your account", @user, data: { turbo_method: :delete, turbo_confirm: "Are you sure?" } %>
</div>